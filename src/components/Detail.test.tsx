import { setup } from "../utils/testUtils"
import { Detail } from "./Detail"

describe("Detail", () => {
    test("the button's label should be 'UNDONE' if the todo is completed", () => {
        // setup(<Detail />)
    })
})